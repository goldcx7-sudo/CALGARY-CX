---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getAllVideos, CATEGORIES, formatDate, youtubeThumbnail } from '../../lib/content';
const videos = getAllVideos();
---
<BaseLayout title="Videos" description="Video content from Calgary CX on CCaaS, CX AI, AI Agents, and contact center operations.">

  <div class="page-hero">
    <div class="container">
      <div class="section-eyebrow">Watch</div>
      <h1>Videos</h1>
      <p class="lead" style="max-width:520px;">Video commentary and analysis on enterprise CX topics. {videos.length} video{videos.length !== 1 ? 's' : ''} published.</p>
    </div>
  </div>

  <section>
    <div class="container">
      {videos.length === 0 ? (
        <div style="text-align:center;padding:5rem 0;">
          <p style="font-size:1.1rem;color:var(--text-3);margin-bottom:1.5rem;">Videos coming soon. Articles are live in the meantime.</p>
          <a href="/articles" class="btn btn-primary">Browse Articles</a>
        </div>
      ) : (
        <div class="video-grid">
          {videos.map(video => (
            <a href={`/videos/${video.slug}`} class="video-card">
              <div class="video-thumb">
                <img src={youtubeThumbnail(video.youtubeId)} alt={video.title} loading="lazy" />
                <div class="video-play-btn">
                  <svg viewBox="0 0 24 24"><path d="M8 5v14l11-7z"/></svg>
                </div>
              </div>
              <div class="video-body">
                <div class="video-meta">
                  <span class={`category-pill cat-${video.category}`}>{video.categoryLabel}</span>
                  {video.duration && <span class="video-duration">{video.duration}</span>}
                </div>
                <div class="video-title">{video.title}</div>
                <p class="video-desc">{video.description}</p>
              </div>
            </a>
          ))}
        </div>
      )}
    </div>
  </section>

</BaseLayout>
